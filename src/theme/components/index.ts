import { Button } from './button';
import { Form, FormLabel } from './forms';
import { Input, NumberInput } from './inputs';
import { Menu } from './menu';
import { Modal } from './modal';
import { Skeleton } from './skeleton';

export const components = {
   Button,
   Form,
   FormLabel,
   Input,
   NumberInput,
   Menu,
   Modal,
   Skeleton,
};
